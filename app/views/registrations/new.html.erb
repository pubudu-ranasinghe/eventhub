<h1>Register for <%= @event.title %></h1>

<%= form_for @reg do |f| %>

  <% if @reg.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reg.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @reg.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group col-md-12">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id, class: 'form-control' %>
  </div>

  <div class="form-group col-md-12">
    <%= f.label :event_id %><br>
    <%= f.text_field :event_id, class: 'form-control' %>
  </div>

<% end %>
